# Configuración de scraping de Prometheus para el OpenTelemetry Collector
# y otros posibles targets del proyecto.

global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
