"""Definición del estado compartido entre los nodos del grafo (LangGraph).

Aquí se define el esquema de estado (TypedDict o modelo Pydantic) que
viaja entre nodos durante la ejecución del workflow del agente.
"""

# TODO: definir el TypedDict/BaseModel con los campos del estado del agente.
